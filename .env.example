# Telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Service
PORT=8080
DATA_DIR=/var/data
LOG_LEVEL=INFO

# Safety: keep 0 until WALLET shows the expected wallet/balance.
LIVE_MASTER_ENABLE=0
POLYMARKET_PRIVATE_KEY=
POLYMARKET_WALLET_ADDRESS=
POLYMARKET_RELAYER_API_KEY=
POLYMARKET_RELAYER_API_KEY_ADDRESS=

# Default position sizes. Telegram SIZE commands override these persistently.
ENTRY_ORDER_SIZE=5
DCA_ORDER_SIZE=5
LIVE_MIN_SHARES=0.01
LIVE_MAX_SHARES_PER_ORDER=1000

# PAPER ledger
PAPER_START_BALANCE=500
MIN_FREE_CASH=5

# Decision timing
DECISION_INTERVAL=3.0
TRADE_WINDOW_SECONDS=180
DISCOVERY_INTERVAL=10
MAX_BOOK_AGE_MS=1000

# FIRST V2-eligible vote tape (all 7 monitored tokens)
V2_ELIGIBLE_PRICE_MIN=0.55
V2_ELIGIBLE_PRICE_MAX=0.75
V2_ELIGIBLE_MOM_MIN=0.03
V2_ELIGIBLE_MOM_MAX=0.30
LOOKBACK_TICKS=2

# G/H target gate (trades only BNB/ETH)
TIGHT_ENTRY_PRICE_MIN=0.67
TIGHT_ENTRY_PRICE_MAX=0.70
SAFE_ENTRY_MOM_MIN=0.05
SAFE_ENTRY_MOM_MAX=0.10
CONSENSUS_WINDOW_SEC=10
CONSENSUS_MIN_OTHER_TOKENS=2

# H safer reversal DCA
DCA_ARM_PRICE=0.50
DCA_MIN_BUY_PRICE=0.30
DCA_MAX_BUY_PRICE=0.60
DCA_REBOUND_MOM_MIN=0.05
DCA_REBOUND_MOM_MAX=0.15
DCA_DEADLINE_SEC=120

MIN_PRICE=0.08
MAX_PRICE=0.95
CRYPTO_FEE_RATE=0.07

# Render/memory stability
MEMORY_CLEANUP_INTERVAL=60
MEMORY_KEEP_RESOLVED_SEC=900
WS_MAX_CONNECTION_AGE_SEC=900
MEMORY_LOG_INTERVAL=300
